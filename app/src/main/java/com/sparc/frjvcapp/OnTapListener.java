package com.sparc.frjvcapp;

public interface OnTapListener {
    void OnTapView(int position, String presKey);
    //Pres key is used to determine that the user tap is on the image view or on the cardview
}
